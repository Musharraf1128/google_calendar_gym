# Example environment configuration for Google Calendar Gym Frontend

# =============================================================================
# SIMULATED POPUPS
# =============================================================================
# Enable random transient notifications and modal dialogs for testing/demo
# When enabled, the application will show:
# - Reminder notifications (toast messages)
# - "Meeting starting" notifications (toast messages)
# - Event detail modals (modal dialogs)
# - Permission denied modals (modal dialogs)
#
# Popups appear every 20-60 seconds with small timing offsets (0-2s)
# to create frame variation for screenshots
#
# Values: true | false
# Default: false (disabled)
VITE_SIMULATE_POPUPS=true
